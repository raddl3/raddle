pre-commit:
  commands:
    prek:
      run: prek run
commit-msg:
  commands:
    commitizen:
      run: uv run --frozen cz check --commit-msg-file {1}
pre-push:
  commands:
    check:
      run: task check
