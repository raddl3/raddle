# Accelerator contract

A product accelerator needs a stable ID and version, a reference and alternate
implementation, declared input/output contracts, validation policy, execution
provenance, supported environment, and reproducible benchmark cases. The
`fixture.sum_squares` descriptor demonstrates shape only. It is not a product
accelerator or performance evidence.
