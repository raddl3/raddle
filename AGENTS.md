# Public Raddle repository

This repository owns the installable package, accelerator contracts, validation,
provenance, CLI, and reproducible benchmark definitions. It must work without
Raddle-hosted services or private dependencies. Use `uv` and the Taskfile for
Python work. Keep fixture accelerators clearly separate from product claims.
Do not commit or push without explicit user instruction.
