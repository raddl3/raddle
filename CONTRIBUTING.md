# Contributing

Run `task bootstrap` once, then `task ci` before a pull request. Commit messages
use Conventional Commits and are checked by Commitizen. The public package
must remain usable without accounts, hosted services, or private packages.
